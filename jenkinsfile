pipeline {
    agent any

    environment {
        VERSION = 'latest'
    }

    stages {
        stage('build') {
            steps {
                sh "echo 'Building version ${VERSION}...'"
            }
        }
    }
}